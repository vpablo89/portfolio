export interface PortfolioProject {
  name: string
  description: string
  problem: string
  stack: string[]
  features: string[]
  learned: string
  githubUrl: string
  demoUrl?: string
  docsUrl?: string
  updatedAt?: string
}
